<main>
  <div class="container">
    <div clsss="row">
      <% @tag_list.each do |list|%>
        <%=list.tag_name %>
        <%="(#{list.articles.count})" %>
      <% end %>

      投稿一覧画面で投稿に紐づくタグを表示
      <% @articles.each do |article| %>
       <%= article.tags.map(&:tag_name).join(",") %>
      <% end %>

    </div>
  </div>
</main>