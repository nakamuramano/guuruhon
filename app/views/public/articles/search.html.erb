<main>
  <div class="container">
    <div class="row">
       <h2>検索結果 "<%= @content %>"</h2>
          <ul>
            <% @articles.each do |article| %>
              <li>
                <%= link_to article_path(article.id) do %>
                  <%= article.title %>
                <% end %>
              </li>
            <% end %>
          </ul>
    </div>
  </div>
</main>