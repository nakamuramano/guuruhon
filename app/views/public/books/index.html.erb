  <% @books.each do |book| %>
    <div class="commic-data">
      <%= link_to (image_tag(book.medium_image_url)), book.item_url %>
      <%= book.title %>
      <%= book.sales_date %>
      <%= book.isbn%>
    </div>
  <% end %>
